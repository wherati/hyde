var mapUrl = require('./mapUrl')
var highlight = require('./highlight')
var changed = require('./changed-with-deps')

module.exports = {
  mapUrl: mapUrl,
  highlight: highlight,
  changed: changed
}
